# Configuración de Producción - CRD Backend
FLASK_APP=app.py
FLASK_ENV=production
DEBUG=False

# BASE DE DATOS (Cambia esto en el servidor SSH)
# Formato: postgresql+psycopg://usuario:password@host:puerto/nombre_db
DATABASE_URL=postgresql+psycopg://postgres:POS_2025@127.0.0.1:5432/Creditos

# SEGURIDAD (Genera una clave larga y aleatoria en producción)
JWT_SECRET_KEY=dev-secret-change-me_CAMBIAME_EN_PRODUCCION
JWT_ACCESS_TOKEN_EXPIRES=3600

# CORS (Orígenes permitidos, separados por coma)
CORS_ALLOWED_ORIGINS=http://187.77.37.231,http://localhost:3000
